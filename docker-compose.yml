services:
  moneytree:
    image: moneytree:latest
    volumes:
      - ./data:/app/data
      - ./backend/uploads:/app/backend/uploads
    ports:
      - "127.0.0.1:8000:8000"
    env_file:
      - .env
    networks:
      - npm_network

networks:
  npm_network:
    external: true
